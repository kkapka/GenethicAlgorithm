using genetic_algorithm.population;
using System;

namespace genetic_algorithm.selection
{
    class LinearRankSelection:ISelection
    {
        public Population select(Population p)
        {
            Console.WriteLine("Linear Rank Selection");
            return null;
        }
    }
}
